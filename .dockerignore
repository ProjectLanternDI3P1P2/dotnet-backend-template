# Leading **/ matters: "obj/" alone only matches the repository root, so the per
# project obj/ folders were copied in and overwrote the restore done in the image.
**/bin/
**/obj/
**/.vs/
**/.vscode/

**/*.user
**/TestResults/
**/coverage/
**/logs/
**/*.log
